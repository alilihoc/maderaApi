{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.gamme) }}
    {{ form_row(form.modules) }}
    <button id="addModule" class="bouton-modifier">Nouveau module</button>
    <button class="btn bouton_lien" style="float: right; margin-right: 10px">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
